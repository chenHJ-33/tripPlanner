spring:
  profiles:
    active: local
  application:
    name: tripPlanner
trip:
  llm:
    model: qwen-max
agentscope:
  dashscope:
    api-key: ${ai.apikey}
mcp:
  unsplash:
    access-key: ${unsplash.accesskey}
    secret-key: ${unsplash.accesssecret}
  gaode:
    key: ${gaode.key}
    # stdio=本地子进程 uvx amap-mcp-server；http=直连高德官方托管端点
    transport: stdio